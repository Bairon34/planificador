import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StyleSheet,
  Text,
  View,
} from 'react-native';

import Header from './src/components/Header';



const App = () => {
  return (
    <SafeAreaView>
      <Header/>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({

});

export default App;
